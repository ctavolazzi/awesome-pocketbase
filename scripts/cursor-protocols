#!/bin/bash
# Cursor Coding Protocols Helper Script
# Quick access to cursor-protocols CLI

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node "$SCRIPT_DIR/cursor-protocols-cli.js" "$@"

